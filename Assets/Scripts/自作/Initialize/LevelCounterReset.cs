using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelCounterReset : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LevelManager.level = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
